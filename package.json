{
  "name": "jasmine-flight",
  "version": "1.1.1",
  "devDependencies": {
    "karma": "~0.8.5"
  },
  "scripts": {
    "test": "./node_modules/.bin/karma start --browsers Firefox --single-run"
  }
}
